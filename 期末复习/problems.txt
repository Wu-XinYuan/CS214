问题：最近点对
提示：分治法
对于2的幂次个点，把他们分成两部分，每部分找出最近点对
之后只考虑距离中轴一定距离内的点（分治得到的最小距离）
由于每个区分布有特色（最小距离），按照y
对比几个（如果按照y轴分治）
简化，把y轴排序改为归并（顺便）
复杂度：O(nlog^2n)；O(nlogn)

问题：线段交点个数
随机n条线段
提示：停顿事件：线段开始，线段结束，交点
按照y顺序维护进度表
事件:
	左端点，加入集合，检查相邻线段交点
	右端点，从集合删除线段，检查相邻线段交点
	交点，交换，检查相邻线段交点
复杂度：O((2n+m)log(2n+m))

问题：Euler公式
提示：V - E + F = 2
V - E + F - C = 1
复杂度：

问题：美术馆问题
需要多少个保安监视美术馆（视线覆盖）
提示：三角划分
存在符合监控要求的保安集合，其中保安个数最多为\lfloor n/3 \rfloor
引理：T是简单多边形三角划分后得到的图，则T可用3种颜色进行有效着色。
复杂度：

问题：多边形三角划分-单调多边形
提示：扫描，增加对角线的方法将左侧部分三角化，并将得到的三角区域丢弃，在后面的计算过程不再考虑
复杂度：O(n)

问题：判断单调多边形：
折线于给定直线L是严格单调的是指对于任意与L垂直的直线与C最多只有一个交点
边界可以被分为两条折线，每条折线关于都是单调的
提示：分成上下两部分验证
复杂度：O(n)

问题：数值分析误差来源
提示：模型：与实际问题之间
观测：测量值
截断：数学模型与数值
舍入：机器字长有限
复杂度：

问题：求幂运算
提示：常规算法，一个个乘
归约，平方
复杂度：指数复杂度
线性复杂度

问题：gcd
提示：辗转相余
复杂度：相对于规模复杂度线性（两次以后必是原来的一半）

问题：多项式乘法
提示：正常算
用(a+b)(c+d)-ac-bd，减少一次乘法
插值，用点的值代替函数
复杂度：O(n2)
O(n^1.59)
O(n^2)

问题：正整数乘法
提示：正常算
用(a+b)(c+d)-ac-bd，减少一次乘法
复杂度：O(n2)
O(n^1.59)

问题：矩阵乘法
提示：普通算法
Winograd算法：减少乘法一半，转化为加法：A=\sum(a_{i,2k-1}*a_{i,2k})
Strassen算法: 把两个2*2矩阵的乘法次数降到7次
复杂度：普通算法：n^3次乘法(n-1)n^2次加法
Winograd:乘法运算量减少至n^3/2+n2
Strassen：n^2.81,n至少需要100才能使Strassen算法快于O(n3) 的直接算法

问题：布尔矩阵乘法
提示：传统不适用，因为没有减
将n切成每段长k的几段
行*列，一个元素时相加，或者列*行，矩阵相加
构造加法表
复杂度：行*列，如果k=[lbn/2],时间O(n3/logn)以及额外空间O(n)
列*行：运行时间为O(n^2)
加法表：O(n^3/log^2n)

问题：矩阵链相乘
提示：dp
复杂度：蛮力 \Omega(4^n/sqrt(n))
dp:时间\Theta(n^3)，空间\Theta(n^2)

问题：正向傅里叶变换
提示：由n个点的值求多项式系数
简化，可以取x_j=-x_i，可以分割成两个小问题
再分割，最终取w^n=1
求w矩阵的变换
复杂度：O(nlogn)

问题：逆向傅里叶变换
提示：利用矩阵的逆，V(w)-1=1/nV(1/w)，变成和正向一样
复杂度：O(nlogn)

问题：非线性方程求根
提示：二分法：f(a)*f(b)<0, 收敛慢
迭代：f(x)=0, g(x)=x，不一定收敛
牛顿法：条件下保证收敛：g(x)=x-f(x)/f'(x)，如果x选的离零点够近
条件：(1)  f(a)*f(b)<0（有根）
(2) 在整个[a, b]上f”不变号且f'(x)不等于0（根唯一）
(3)  选取 x0属于[a, b] 使得f(x0)*f''(x0)>0
复杂度：牛顿，某个邻域内，不小于二阶的趋向于0（洛必达）

问题：CSM(循环平移问题）
提示：通过KMP归约
判断B是不是AA的子串
复杂度：线性

问题：不同代表集（SDR）
用R{r1,r2,…,rk}代表S1,S2,…Sk
ri属于Si
提示：Hall定理：对任何m个集合，包含的元素大于m个不同的元素
归约：偶图匹配：一个图离放代表集合的点，另一个放代表元素的点
复杂度：

问题：序列比较
提示：6.8方法，dp
看成有向图，变成单源最短路径
复杂度：O(mn)
？

问题：在无向图中寻找三角形：
提示：归约：布尔矩阵相乘，临界矩阵A[i][j]=1且A^2[i][j]>0
复杂度：直接：O(n^3)
可以O(n^2)

问题：网络流量
提示：归约到与线性规划
复杂度：

问题：判断点是否在图形内部
提示：与图形外一点相连，与线段的交点个数是否为偶数
复杂度：O(n)

问题：构造简单多边形
提示：角度旋转扫描
按照角度排序
复杂度：O(nlogn)

问题：凸包
提示：普通方法：
归纳
找到前n个点的凸包，判断第n+1个点是否在先前的凸包内，更新
	
礼物包裹算法：
不断寻找凸路径
支撑线：所有点都在这根线的同一侧，与图形只有一个交点
不断寻找下一条支撑线，（角度最小）
	
graham扫描算法：
选一个极端点（一般x最大，有一致的y最小）
计算每一点角度
根据角度排序
根据顺序连接每一个点，如果大于180度跳过
复杂度：O(n^2)
O(n^2)
O(nlogn)

问题：DFS
提示：DFS
	v
	for all neighbor w
		DFS(v)
无向图构造的树为F，原来的边要不在F中，要不连接F中的一个祖先与后代
有向图：分为树边、后蜕变、前向边、交叉边
复杂度：O(|V|+|E|)

问题：有向图是否存在闭链
提示：构造DFS树，存在后退边，则存在闭链
遍历时给点加一个属性:onpath，如果后继中有onpath=true的就存在环
复杂度：O(|V|+|E|)

问题：欧拉图
提示：所有边度数都是偶数，可以通过每条边一次回到原点
复杂度：

问题：BFS
提示：BFS
	mark v;
	put v in queue
	while !queue.empty
		pophead(w)
		for all (w,x)
			mark x
			put x in queue
如果u是w在BFS中的父母，则所有导向w的边中，u具有最小的BFS数
从根到某点的最短路径一定是BFs树上路径
如果(v，w)是E中的一条边且不属于T，则它连接的两个顶点的层数至多相差1
复杂度：

问题：拓扑排序
提示：从1到n给顶点标记，从k出发能到的点都大于k
找到入度为0的点，标记1，删除，重复
复杂度：O(|V|+|E|)

问题：单源最短路径-非循环
提示：从v到有向图上任意一点的最短路径
拓扑排序
从v开始一个个递增，研究所有有w的边
复杂度：O(|V|+|E|)

问题：单源最短路径-一般
提示：找到从v出发最短边v,x
到x的最短路径一定是len(v,x)
一个个按照长度递增顺序递归，同时更新剩下边的最短路径堆，每次取出堆顶
复杂度：O((|E| +|V|)log|V|)

问题：最小代价生成树
提示：性质：具有最小代价的边一定在最小代价生成树中
归纳增加时，找一条边把新的点连到原有的树
复杂度：O((|E| +|V|)log|V|)

问题：全部最短路径
提示：将顶点标号
计算k-path（边上最大点为k)
三层循环（两个端点、新加的点）
复杂度：O(|V|3)
稀疏情况下可以执行|V|次单源最短路径

问题：传递闭包
提示：如果v到w有路径，那么在传递闭包中有边
类似全部最短
复杂度：O(|V|3)

问题：双连通分支（无向图）
边的集合
每两个点间至少两条路径（无向图），仅含一条边也是双连通的
提示：标记DFS顺序
寻找关节点
复杂度：O(|V|+|W|)

问题：强连通分支（有向图）
每两个点双向互通，两个点属于同一个强连通分支当且仅当有包含它们的回路
点的集合
提示：新增一条边时，连接的两个点来自同一分支则不影响
DFS，如果更低的边指向更高的边，产生环
与双连通分支区别，栈的清空在遍历完一条边以后还是遍历完全部边以后
复杂度：O(|V|+|W|)

问题：无向图分解
无向连通图是否触怒在偶数长度的闭链
提示：闭链必定在双连通分支中
在双连通分支中找，如果不止一条闭链，那么一定有偶数闭链
复杂度：

问题：有向图是否存在奇数长度闭链
提示：闭链必定在双连通分支中
在双连通分支中找，如果不止一条闭链，那么一定有偶数闭链
复杂度：

问题：非常稠密图中的完美匹配
提示：两个不在已有覆盖中的点
构造图形，两边替一边
复杂度：

问题：偶图匹配
提示：交叉路径，一条在M中，一条不在M中
复杂度：O(n)?

问题：哈密尔顿回路
简单回路，包含V中的点恰好一次
在稠密图中寻找:d(v)+d(w)>=n
提示：从完全边开始，反向归纳（m+1边对，m边也对）
复杂度：

问题：二分搜索
提示：适合有规律的队列，循环队列，递增序列，寻找ai =i
复杂度：O(logn)

问题：内插搜索
提示：相比于二分，缩小得可能更快
适合分布均匀的序列
复杂度：

问题：桶排序
提示：足够多的桶
需要知道范围
复杂度：O（m+n)

问题：基数排序
提示：	给1-10000排序
50个桶，每层10个
第一层搜索结束后，每个依次进入下层
复杂度：o(nk)

问题：插入排序
提示：即使采用二分搜索插入的位置，数据的移动次数仍是O(n^2)的。
通过平衡树优化，插入变成logn，最后从AVL获得序列为O(n)，所以总时间复杂度为O(nlogn)
复杂度：最好O(nlogn)

问题：选择排序
提示：与插入排序相比，根据数据的特征选择第n个药插入的，比如把第n大的n的位置上
复杂度：O（n^2)

问题：归并排序
提示：归并两个有序数组时间复杂度O(m+n)
复杂度：T(2n)=2T(n)+O(n)，解是O(nlogn)

问题：快速排序
提示：
复杂度：最坏O(n2)，平均O(nlogn)

问题：堆排序也是O(logn)的
提示：维护一个最大堆O(n)
复杂度：O(nlogn)，建堆

问题：决策树
提示：用于判断算法下界
复杂度：

问题：计数排序
提示：
复杂度：O(n)

问题：最大最小数
提示：直接比较 2n-3次
两两组队，xn-1先和xn比，小的与min，大的与max比，大约3n/2次
复杂度： 2n-3，3n/2

问题：查找第k小的数
提示：循环n次查找最小数
排序
假快排 
复杂度：O(nk), ,O(n）

问题：数据压缩
提示：哈夫曼树
每个叶子节点对应一个数据
复杂度：

问题：串匹配
提示：KMP
复杂度：O(n)

问题：查找众数
出现次数大于2/n
提示：排序
中数查找
如果有众数，那么删掉两个不同的数之后，依然为众数
复杂度：最后一种O(n)

问题：最长递增序列
提示：递归，维护BIS{K)长度为k的后缀最小的序列
复杂度：O(nlogn)
查找BIS(i+1)=BIS(i)+x_m的i的位置要O(logn)

问题：查找集合中最大的两个元素
提示：直接比较：2n-3
简单分治：3n/2
两个集合合并成一个集合，最大数取两个最大的那个，候选集取最大对应的候选集加上两一个最大数
复杂度：n+logn-2

问题：频繁元素
提示：近似算法
类似LRU?
复杂度：

问题：多项式求值
提示：基础：Pn(x)= Pn-1(x)+anxn
优化：Pn(x)=xP/n-1(x)+a0
复杂度：

问题：最大导出子图
提示：删除所有度数小于K的点
复杂度：

问题：寻找一对一映射
提示：没有被任何元素映射到的元素一定不属于最后的集合，并且去掉时不用取舍（相比于被多个映射到的）
反向考虑，不是直接考虑那些映射到同一元素的
复杂度：

问题：社会名流问题
所有人都认识他，但不认识别人
提示：倒推，排除不是名流的人
复杂度：O(n)，还要验证一遍

问题：轮廓问题
提示：分治
复杂度：O(nlogn)

问题：二叉树平衡因子
提示：加强递归内容：高度差和高度
复杂度：

问题：最大连续子序列
寻找和最大的子序列，空序列之和为0
提示：增强递归：最大子序列以及最大后缀子序列
复杂度：

问题：背包问题
提示：动态规划
知道a(I,k)
复杂度：O(nk)

问题：分治关系
提示：T(n)=aT(n/b)+cn^k
复杂度：

问题：图上无重边
O是V中度数为奇数的结点的集合，证明可以把O中的结点分成结点对，在每一对中都能找到连接这两个结点的无重边的路。
提示：对边数归纳，直接去掉边后不连通
强化假设：所有边数小于m的无向图成立
复杂度：

问题：SAT，证明NPC
提示：输入上的操作可以由一个布尔表达式表述
复杂度：

问题：cook定理
提示：SAT问题是完全NP的
复杂度：

问题：团->顶点覆盖
提示：G上的k的团问题归纳到补图上n-k的覆盖
设C=(U,F)是G中的团，那么所有与U相连的边都在C中，所以V-U就是覆盖
所以V-D是团
复杂度：

问题：NP完全证明
提示：证明属于NP，归约，多项式时间
复杂度：

问题：顶点覆盖->支配集
提示：从G构造G':把每条边变为三角形
G有小于等于k的覆盖当且仅当G'有特定大小的支配集
可以假设D中只有原顶点
复杂度：

问题：SAT->3SAT
提示：3个及以上变量变成3变量之积
2变量子句
1变量子句
复杂度：

问题：SAT->团问题
提示：每个子句中的点不相连，不同子句中的点，除非是反，否则一定相连
可满足当且仅当有大小为m的团
复杂度：

问题：3SAT->3着色
提示：主三角形：T, F, A
每个变量一个三角形: x, x反，A
在每个变量与T之间引入新节点O,再在O里面引入I,每个I相连
复杂度：

问题：一维箱柜问题-近似算法
设x1,x2…,xn， 构成大小介于 0 和 1 之间的实数集合； 将这些数划分成尽可能少的子集合使得每个子集中的数的总和不超过 1
提示：first fit
优化：递减首次适应
复杂度：	OPT为最少箱柜数，那么first fit 算法得到结果至多2OPT
实际上大概1.7

问题：欧几里得旅行商
提示：最小代价生成树，总路程时问题下界，上界是总代价两倍
改进：走捷径，生成欧拉图（匹配）
复杂度：改进后，不会超过1.5OP

问题：顶点覆盖-近似算法
提示：取极大匹配M,取所有M相关的点即为一个覆盖（外面不可能有一个不与这些点相连的点）
复杂度：不会超过最小覆盖的两倍

问题：0、1背包问题（回溯剪枝）
提示：先求出上下界
然后按照性价比进行放置（贪心），维护最大期望，已有价值+剩余重量*剩余最大性价比，每次调期望最大进行检查
复杂度：

问题：单调多边形三角形划分
提示：扫描，增加对角线的方法将左侧部分三角化，并将得到的三角区域丢弃，在后面的计算过程不再考虑
分上下两条，异侧点直接连，同侧如果超过180度则放弃
复杂度：O(n)

问题：一般多边形三角划分
提示：划分为简单多边形
两条边都在左侧：合并点，都在右侧：分离点
分离点，连接在它上面的第一条线的helper(位于这条线和它下方的线中最右侧的点）
扫描线事件点：分离点（加线，两条线入表，改变helper)，合并点（两条线出表，改helper)，起点（两条线入表，置helper)，上折线顶点，下折线顶点
反向扫描处理合并点
复杂度：要排序，O(nlogn)

问题：半平面交集
提示：分治法
复杂度：O(nlogn)

问题：凸多边形交集
提示：扫描线
复杂度：O(n)

问题：Voronoi图
提示：半平面相交
fortune,扫描线，顶点事件，基点事件
复杂度：O(n^2logn),O(nlogn)

问题：
提示：
复杂度：